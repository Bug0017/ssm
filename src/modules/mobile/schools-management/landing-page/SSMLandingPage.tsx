import Layout from "../../common/layout"

// eslint-disable-next-line import/no-anonymous-default-export
export default ()=>{
    
    return (
        <Layout>
            div
        </Layout>
    )
}